<Window x:Class="TaskPlannerClient.Views.TaskDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:TaskPlannerClient.Views"
        mc:Ignorable="d"
        Title="Детали задачи" Height="650" Width="670"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="1.15*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="0.7*"/>
                <RowDefinition/>
                <RowDefinition Height="0.7*"/>
                <RowDefinition/>
                <RowDefinition Height="0.7*"/>
                <RowDefinition/>
                <RowDefinition Height="0.7*"/>
                <RowDefinition/>
                <RowDefinition Height="0.7*"/>
                <RowDefinition Height="3.5*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <!-- Название -->
            <TextBlock Grid.ColumnSpan="3">
                <TextBlock Text="Название: " FontWeight="Bold" Grid.Row="0"/>
                <TextBlock x:Name="TitleText"/>
            </TextBlock>
            

            <!-- Тип -->
            <TextBlock Text="Тип:" FontWeight="Bold" Grid.Row="1" Grid.Column="0"/>
            <TextBlock x:Name="TypeText" Grid.Row="2" Grid.Column="0" VerticalAlignment="Top"/>

            <!-- Приоритет -->
            <TextBlock Text="Приоритет:" FontWeight="Bold" Grid.Row="1" Grid.Column="1"/>
            <TextBlock x:Name="PriorityText" Grid.Row="2" Grid.Column="1"/>

            <!-- Оценка / Факт -->
            <TextBlock Text="Оценка/факт (часы):" Grid.Row="3" FontWeight="Bold"/>
            <TextBlock Grid.Row="4">
                <TextBlock x:Name="EstimateText"/>
                <TextBlock Text="/"/>
                <TextBlock x:Name="ActualText"/>
            </TextBlock>

            <!-- Спринт -->
            <TextBlock Text="Спринт:" FontWeight="Bold" Grid.Column="1" Grid.Row="3"/>
            <TextBlock x:Name="SprintText" Grid.Row="4" Grid.Column="1"/>

            <!-- Исполнитель -->
            <TextBlock Text="Исполнитель:" FontWeight="Bold" Grid.Column="2" Grid.Row="3"/>
            <TextBlock x:Name="AssigneeText" Grid.Column="3" Grid.Row="4"/>

            <!-- Создана -->
            <TextBlock Text="Создана:" Grid.Row="5" FontWeight="Bold" Grid.Column="0"/>
            <TextBlock x:Name="CreatedAtText" Grid.Row="6" Grid.Column="0"/>

            <!-- Обновлена -->
            <TextBlock Text="Обновлена:" FontWeight="Bold" Grid.Column="1" Grid.Row="5"/>
            <TextBlock x:Name="UpdatedAtText" Width="200" Grid.Column="1" Grid.Row="6"/>

            <!-- Дедлайн -->
            <TextBlock Text="Дедлайн:" FontWeight="Bold" Grid.Column="2" Grid.Row="5"/>
            <TextBlock x:Name="DeadlineText" Grid.Column="2" Grid.Row="6"/>

            <!-- Проект -->
            <TextBlock Text="Проект:" FontWeight="Bold" Grid.Row="7" Grid.ColumnSpan="3"/>
            <TextBlock x:Name="ProjectText" Grid.Row="8" Grid.ColumnSpan="3"/>

            <!-- Описание (только чтение для Worker) -->
            <TextBlock Text="Описание:" FontWeight="Bold" Grid.Row="9" Grid.ColumnSpan="3"/>
            <ScrollViewer Grid.Row="10" Grid.ColumnSpan="3">
                <TextBox x:Name="DescriptionTextBox" TextWrapping="Wrap" AcceptsReturn="True"
             Style="{StaticResource MaterialDesignOutlinedTextBox}" VerticalScrollBarVisibility="Auto"/>
            </ScrollViewer>
        </Grid>

        <!-- Заметки (сворачиваемый блок) -->
        <Expander Grid.Row="1" Header="Заметки" IsExpanded="True" Margin="0,10,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Поле ввода -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBox x:Name="NoteTextBox" Width="500" Margin="3"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             materialDesign:HintAssist.Hint="Текст заметки..."/>
                    <Button x:Name="AddNoteButton" Content="Добавить" Margin="10,0,0,0"
                            Style="{StaticResource MaterialDesignRaisedButton}" Click="AddNoteButton_Click"/>
                </StackPanel>

                <!-- Список заметок -->
                <ScrollViewer Grid.Row="1" MaxHeight="150" VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="NotesItemsControl">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{DynamicResource MaterialDesignCardBackground}"
                                        CornerRadius="4" Padding="8" Margin="0,0,0,5">
                                    <Grid Margin="3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding Content}" TextWrapping="Wrap"/>
                                            <TextBlock FontSize="11" Foreground="Gray" Margin="0,3,0,0">
                                                <Run Text="{Binding Author.LastName}"/>
                                                <Run Text=" "/>
                                                <Run Text="{Binding Author.FirstName}"/>
                                                <Run Text=" ("/>
                                                <Run Text="{Binding CreatedAt, StringFormat='dd.MM.yyyy HH:mm'}"/>
                                                <Run Text=")"/>
                                            </TextBlock>
                                        </StackPanel>
                                        <Button Grid.Column="1" Content="✕" ToolTip="Удалить"
                                                Style="{StaticResource MaterialDesignIconButton}"
                                                Click="DeleteNoteButton_Click" Tag="{Binding}"
                                                VerticalAlignment="Top"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Expander>

        <!-- Кнопки действий -->
        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,10,0,0" HorizontalAlignment="Right">
            <ComboBox x:Name="StatusComboBox" Width="150" Margin="0,0,10,0"
                      Style="{StaticResource MaterialDesignOutlinedComboBox}"
                      DisplayMemberPath="Name" SelectedValuePath="Id"/>
            <Button x:Name="SaveButton" Content="Сохранить" Margin="0,0,10,0"
                    Style="{StaticResource MaterialDesignRaisedButton}" Click="SaveButton_Click"/>
            <Button x:Name="EditButton" Content="Редактировать" Margin="0,0,10,0"
                Style="{StaticResource MaterialDesignRaisedButton}" Click="EditButton_Click"
                HorizontalAlignment="Left"/>
            <Button x:Name="TakeButton" Content="Взять задачу" Margin="0,0,10,0"
                    Style="{StaticResource MaterialDesignRaisedButton}" Click="TakeButton_Click"/>
            <Button Content="Отмена" IsCancel="True" Style="{StaticResource MaterialDesignFlatButton}"/>
        </StackPanel>
    </Grid>
</Window>
