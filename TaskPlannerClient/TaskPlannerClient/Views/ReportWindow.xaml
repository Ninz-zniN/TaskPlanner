<Window x:Class="TaskPlannerClient.Views.ReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskPlannerClient.Views"
        mc:Ignorable="d"
        Title="Отчёты" Height="700" Width="1000"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Отчёты" FontSize="20" FontWeight="Bold" Grid.Row="0" Margin="0,0,0,10"/>

        <TabControl x:Name="ReportsTabControl" Grid.Row="1"
                    Style="{StaticResource MaterialDesignTabControl}"/>

        <!-- Панель кнопок экспорта -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="ExportPdfButton" Content="Экспорт PDF" 
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Click="ExportPdfButton_Click"/>
            <Button x:Name="ExportExcelButton" Content="Экспорт Excel" 
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Margin="10,0,0,0" Click="ExportExcelButton_Click"/>
        </StackPanel>
        <!-- Панель с кнопкой для Burn‑down диаграммы -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="OpenBurndownButton" Content="Открыть Burn‑down диаграмму" 
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Click="OpenBurndownButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
