<Window x:Class="TaskPlannerClient.Views.TeamEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskPlannerClient.Views"
        mc:Ignorable="d"
        Title="Команда" Height="300" Width="500"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Название команды" FontWeight="Bold" Grid.Row="0"/>
        <TextBox x:Name="TeamNameTextBox" Grid.Row="1" Margin="0,5,0,10"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>

        <TextBlock Text="Тимлид" FontWeight="Bold" Grid.Row="2"/>
        <ComboBox x:Name="TeamLeadComboBox" Grid.Row="3" Margin="0,5,0,10"
                  Style="{StaticResource MaterialDesignOutlinedComboBox}"
                  SelectedValuePath="IdEmployee">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding LastName}"/>
                        <TextBlock Text=" "/>
                        <TextBlock Text="{Binding FirstName}"/>
                        <TextBlock Text=" ("/>
                        <TextBlock Text="{Binding Position}"/>
                        <TextBlock Text=")"/>
                    </StackPanel>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="5" Margin="0,10,0,0">
            <Button x:Name="SaveButton" Content="Сохранить" Style="{StaticResource MaterialDesignRaisedButton}"
                    Click="SaveButton_Click" Margin="0,0,10,0"/>
            <Button Content="Отмена" IsCancel="True" Style="{StaticResource MaterialDesignFlatButton}"/>
        </StackPanel>
    </Grid>
</Window>
