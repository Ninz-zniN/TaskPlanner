<UserControl x:Class="TaskPlannerClient.Views.Tabs.Manager.AccuracyReportControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TaskPlannerClient.Views.Tabs.Manager"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="SummaryText" FontSize="14" FontWeight="Bold" Grid.Row="0" Margin="0,0,0,10"/>

        <!-- Выбор спринта -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <ComboBox x:Name="SprintFilterCombo" Style="{StaticResource MaterialDesignOutlinedComboBox}"
                      DisplayMemberPath="SprintName" SelectedValuePath="IdSprint"
                      materialDesign:HintAssist.Hint="Спринт"
                      Width="200" SelectionChanged="SprintFilter_SelectionChanged"/>
            
            <Button Content="История статусов" Click="OpenStatusHistoryButton_Click" Style="{StaticResource MaterialDesignOutlinedButton}" Margin="10,0,0,0" />
        </StackPanel>

        <DataGrid x:Name="AccuracyDataGrid" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Задача" Binding="{Binding Task.Title}" Width="*"/>
                <DataGridTextColumn Header="Оценка (ч)" Binding="{Binding EstimateHours}" Width="100"/>
                <DataGridTextColumn Header="Факт (ч)" Binding="{Binding ActualHours}" Width="100"/>
                <DataGridTextColumn Header="Отклонение (ч)" Binding="{Binding Deviation}" Width="130"/>
                <DataGridTextColumn Header="Отклонение %" Binding="{Binding DeviationPercent, StringFormat='{}{0:F1}%'}" Width="130"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock x:Name="TotalText" Grid.Row="3" FontSize="12" Margin="0,10,0,0"/>
    </Grid>
</UserControl>
