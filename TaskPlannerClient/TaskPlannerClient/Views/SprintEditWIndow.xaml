<Window x:Class="TaskPlannerClient.Views.SprintEditWIndow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskPlannerClient.Views"
        mc:Ignorable="d"
        Title="Спринт" Height="350" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Название" FontWeight="Bold" Grid.Row="0"/>
        <TextBox x:Name="SprintNameTextBox" Grid.Row="1" Margin="0,5,0,10"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>

        <TextBlock Text="Дата начала" FontWeight="Bold" Grid.Row="2"/>
        <DatePicker x:Name="StartDatePicker" Grid.Row="3" Margin="0,5,0,10"
                    Style="{StaticResource MaterialDesignOutlinedDatePicker}"/>
        
        <TextBlock Text="Дата окончания" FontWeight="Bold" Grid.Row="4"/>
        <Grid Grid.Row="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <DatePicker x:Name="EndDatePicker" Margin="0,5,0,10"
                Style="{StaticResource MaterialDesignOutlinedDatePicker}"/>
            <TextBox x:Name="WorkDaysTextBox" Grid.Row="3" Margin="0,5,0,10" Width="50"
                HorizontalAlignment="Right" Style="{StaticResource MaterialDesignOutlinedTextBox}" Grid.Column="1"/>
        </Grid>

        <TextBlock Text="Рабочих дней" FontWeight="Bold" Grid.Row="4" HorizontalAlignment="Right" Margin="0,0,0,0"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="6">
            <Button x:Name="SaveButton" Content="Сохранить" 
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Click="SaveButton_Click" Margin="0,0,10,0"/>
            <Button Content="Отмена" IsCancel="True"
                    Style="{StaticResource MaterialDesignFlatButton}"/>
        </StackPanel>
    </Grid>
</Window>
