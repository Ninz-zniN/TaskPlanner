<Window x:Class="TaskPlannerClient.Views.BurndownReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskPlannerClient.Views"
        mc:Ignorable="d"
        Title="Burndown диаграмма" Height="450" Width="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Выбор спринта -->
        <ComboBox x:Name="SprintCombo" Grid.Row="0"
                  Style="{StaticResource MaterialDesignOutlinedComboBox}"
                  materialDesign:HintAssist.Hint="Спринт"
                  DisplayMemberPath="SprintName"
                  SelectedValuePath="IdSprint"
                  Width="300"
                  SelectionChanged="SprintCombo_SelectionChanged" HorizontalAlignment="Left"/>

        <TextBlock x:Name="SprintTitle" FontSize="16" FontWeight="Bold" Grid.Row="1" Margin="0,0,0,10"/>

        <!-- Одинокий график во всём окне -->
        <lvc:CartesianChart x:Name="BurndownChart" Grid.Row="2"
                            Series="{Binding Series}"
                            XAxes="{Binding XAxes}"
                            YAxes="{Binding YAxes}"
                            TooltipPosition="Top"
                            LegendPosition="Right"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"/>
    </Grid>
</Window>
