<Window x:Class="TaskPlannerClient.Views.UserEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskPlannerClient.Views"
        mc:Ignorable="d"
        Title="Пользователь" Height="450" Width="500">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Логин" FontWeight="Bold" Grid.Row="0"/>
        <TextBox x:Name="LoginTextBox" Grid.Row="1" Margin="0,5,0,10"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>

        <TextBlock Text="Пароль" FontWeight="Bold" Grid.Row="2"/>
        <TextBox x:Name="PasswordTextBox" Grid.Row="3" Margin="0,5,0,10"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>

        <TextBlock Text="Роль" FontWeight="Bold" Grid.Row="4"/>
        <ComboBox x:Name="RoleComboBox" Grid.Row="5" Margin="0,5,0,10"
                  Style="{StaticResource MaterialDesignOutlinedComboBox}"
                  SelectedValuePath="Content">
            <ComboBoxItem Content="worker"/>
            <ComboBoxItem Content="project_manager"/>
            <ComboBoxItem Content="admin"/>
        </ComboBox>

        <TextBlock Text="Сотрудник" FontWeight="Bold" Grid.Row="6"/>
        <ComboBox x:Name="EmployeeComboBox" Grid.Row="7" Margin="0,5,0,10"
                  Style="{StaticResource MaterialDesignOutlinedComboBox}"
                  SelectedValuePath="IdEmployee"
                  VerticalAlignment="Top">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding LastName}"/>
                        <TextBlock Text=" "/>
                        <TextBlock Text="{Binding FirstName}"/>
                        <TextBlock Text=" ("/>
                        <TextBlock Text="{Binding Position}"/>
                        <TextBlock Text=")"/>
                    </StackPanel>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="8" Margin="0,10,0,0">
            <Button x:Name="SaveButton" Content="Сохранить" Style="{StaticResource MaterialDesignRaisedButton}"
                    Click="SaveButton_Click" Margin="0,0,10,0"/>
            <Button Content="Отмена" IsCancel="True" Style="{StaticResource MaterialDesignFlatButton}"/>
        </StackPanel>
    </Grid>
</Window>
